class Contatos {
    constructor(id_professor, discord, whatsapp, teams) {
      this.id_professor = id_professor;
      this.discord = discord;
      this.whatsapp = whatsapp;
      this.teams = teams;
    }
};

module.exports = Contatos;